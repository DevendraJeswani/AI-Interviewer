# URL of the deployed backend — no trailing slash.
# In local development this can be left empty; Vite's dev proxy handles it.
# In production (Vercel / Netlify) set this to your Railway / Render backend URL.
# Example: VITE_API_URL=https://ai-interviewer-production.up.railway.app
VITE_API_URL=
