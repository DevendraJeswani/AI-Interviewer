# URL of the deployed backend — no trailing slash.
# In local development this can be left empty; Vite's dev proxy handles it automatically.
# In production (Netlify / Vercel) set this to your Render / Railway backend URL.
VITE_API_URL=https://ai-interviewer-mz3b.onrender.com
